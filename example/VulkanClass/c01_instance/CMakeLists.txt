SET(target_ c01_instance)

SET(SOURCES_FILES main.cpp)
ADD_EXECUTABLE(${target_} ${SOURCES_FILES})

TARGET_INCLUDE_DIRECTORIES(${target_} PRIVATE ${VULKAN_INCLUDE_DIR})
TARGET_LINK_LIBRARIES(${target_}  PRIVATE ${VULKAN_LIBRARY} glfw)


