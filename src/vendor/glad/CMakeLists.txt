SET(target_ glad)
SET(EXECUTABLE_OUTPUT_PATH ${CMAKE_SOURCE_DIR}/bin)

FILE(GLOB FILES ${CMAKE_CURRENT_SOURCE_DIR}/*.*)

# 生成可执行文件
ADD_LIBRARY(${target_} STATIC ${FILES})

TARGET_INCLUDE_DIRECTORIES(
        ${target_}
        PRIVATE
        ${CMAKE_CURRENT_SOURCE_DIR}/include
)
