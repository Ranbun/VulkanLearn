## 5.1 物理设备
- Go:  [物理设备-Doc][Vulkan® 1.3.259 - A Specification - PhysicalDevices]

&ensp;&ensp;要获取系统中已安装的物理设备的物理设备对象列表，请调用：

```C++
// Provided by VK_VERSION_1_0
VkResult vkEnumeratePhysicalDevices(
    VkInstance                                  instance,
    uint32_t*                                   pPhysicalDeviceCount,
    VkPhysicalDevice*                           pPhysicalDevices);

// instance must be a valid VkInstance handle
// pPhysicalDeviceCount must be a valid pointer to a uint32_t value
// If the value referenced by pPhysicalDeviceCount is not 0, and pPhysicalDevices is not NULL, pPhysicalDevices must be a valid pointer to an array of pPhysicalDeviceCount VkPhysicalDevice handles
```

&ensp;&ensp;查询物理设备的通用属性可以使用以下接口：
```C++
// Provided by VK_VERSION_1_0
void vkGetPhysicalDeviceProperties(
    VkPhysicalDevice                            physicalDevice,  // 物理设备句柄。
    VkPhysicalDeviceProperties*                 pProperties      // 指向VkPhysicalDeviceProperties结构体的指针
);
// physicalDevice must be a valid VkPhysicalDevice handle
// pProperties must be a valid pointer to a VkPhysicalDeviceProperties structure
```

&ensp;&ensp;或是使用后续版本引入的接口:

```C++
// Provided by VK_VERSION_1_1
void vkGetPhysicalDeviceProperties2(
    VkPhysicalDevice                            physicalDevice,  // 物理设备句柄。
    VkPhysicalDeviceProperties2*                pProperties      // 一个指向VkPhysicalDeviceProperties2结构体的指针
);

// physicalDevice must be a valid VkPhysicalDevice handle
// pProperties must be a valid pointer to a VkPhysicalDeviceProperties2 structure
```












[Vulkan® 1.3.259 - A Specification - PhysicalDevices]:https://registry.khronos.org/vulkan/specs/1.3/html/chap5.html#devsandqueues-physical-device-enumeration	"物理设备"
